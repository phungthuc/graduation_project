PlayerManager
    -Orientation
    -Head
    -Camera (MoveCamera script)
        -CameraPivot
            -CameraContainer
                -Main Camera (Contain Camera object, CameraFOVManager script, CamShake script, Post-process Layer object, UniversalAdditionalCameraData script)
                    -WeaponCamera (Camera object and UniversalAdditionalCameraData script)
                        - JumpMotion
                            - .. Gun, arm,..